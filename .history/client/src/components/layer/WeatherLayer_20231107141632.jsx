import React from "react";
import keys from "./key.js";

const WeatherLayer = () => {
  const api = {
    key: keys.API_KEY,
    base: keys.BASE_URL,
  };
  return <div></div>;
};

export default WeatherLayer;
